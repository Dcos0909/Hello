<%@ page contentType="text/html;charset=UTF-8" %>
<html lang="en" itemscope itemtype="https://schema.org/Person">
<head>
    <!-- Primary Meta Tags -->
    <title>Ritika | Full Stack Developer | Spring Boot Specialist</title>
    <meta name="description" content="Professional portfolio of Ritika, Senior Java Full Stack Developer with 5+ years experience in Spring Boot, Microservices, and modern web development.">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="https://www.ritikadev.com/" />
    
    <!-- Open Graph / Facebook Meta Tags -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://www.ritikadev.com/">
    <meta property="og:title" content="Ritika | Senior Java Full Stack Developer">
    <meta property="og:description" content="Professional portfolio of Ritika, Senior Java Full Stack Developer specializing in Spring Boot and modern web technologies.">
    <meta property="og:image" content="https://www.ritikadev.com/images/social-preview.jpg">
    
    <!-- Twitter Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="https://www.ritikadev.com/">
    <meta name="twitter:title" content="Ritika | Senior Java Full Stack Developer">
    <meta name="twitter:description" content="Professional portfolio of Ritika, Senior Java Full Stack Developer specializing in Spring Boot and modern web technologies.">
    <meta name="twitter:image" content="https://www.ritikadev.com/images/social-preview.jpg">
    <meta name="twitter:creator" content="@ritikadev">
    
    <!-- Structured Data -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Ritika",
      "jobTitle": "Senior Java Full Stack Developer",
      "url": "https://www.ritikadev.com",
      "sameAs": [
        "https://github.com/ritikadev",
        "https://linkedin.com/in/ritikadev",
        "https://twitter.com/ritikadev"
      ],
      "description": "Professional Java Full Stack Developer with expertise in Spring Boot, Microservices, and modern web development.",
      "image": "https://www.ritikadev.com/images/profile.jpg"
    }
    </script>
    
    <!-- Viewport and Mobile Optimization -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="theme-color" content="#4e54c8">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Ritika Dev">
    
    <!-- Favicon Configuration -->
    <link rel="icon" type="image/svg+xml" href="https://cdn.jsdelivr.net/gh/twitter/twemoji@14.0.2/assets/svg/2615.svg">
    <link rel="icon" href="/favicon.ico" type="image/x-icon" sizes="any">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest" crossorigin="use-credentials">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#4e54c8">
    
    <!-- Preload Critical Resources -->
    <link rel="preload" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" as="style">
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@700&display=swap" as="style">
    <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" as="style">
    
    <!-- DNS Prefetch -->
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
    <link rel="dns-prefetch" href="https://fonts.googleapis.com">
    <link rel="dns-prefetch" href="https://cdnjs.cloudflare.com">
    
    <!-- Bootstrap 5 with dark mode support -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-dark-5@1.1.3/dist/css/bootstrap-dark.min.css" integrity="sha384-wG4B6H5R/kXp6YJZV5Y2Z5F5z5F5z5F5z5F5z5F5z5F5z5F5z5F5z5F5z5F5z5F5" crossorigin="anonymous">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@700&display=swap" rel="stylesheet">
    
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" integrity="sha512-c42qTSw/wPZ3/5LBzD+Bw5f7bSF2oxou6wEb+I/lqeaKV5FDIfMvvRp772y4jcJLKuGUOpbJMdg/BTl50fJYAw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- AOS (Animate On Scroll) -->
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    
    <!-- Critical CSS (Inlined for performance) -->
    <style>
        :root {
            --primary-color: #4e54c8;
            --primary-dark: #3a40a8;
            --secondary-color: #8f94fb;
            --accent-color: #ff6b6b;
            --dark-color: #121212;
            --darker-color: #0d0d0d;
            --light-color: #f8f9fa;
            --transition-time: 0.4s;
            --header-height: 80px;
            --max-content-width: 1440px;
            --border-radius: 12px;
            --box-shadow: 0 12px 24px -6px rgba(78, 84, 200, 0.16);
            --box-shadow-hover: 0 16px 32px -8px rgba(78, 84, 200, 0.24);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html {
            scroll-behavior: smooth;
            scroll-padding-top: calc(var(--header-height) + 20px);
        }
        
        body {
            font-family: 'Poppins', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: var(--light-color);
            color: #333;
            transition: all var(--transition-time) ease;
            overflow-x: hidden;
            line-height: 1.6;
            text-rendering: optimizeLegibility;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        
        .dark-mode {
            background-color: var(--dark-color);
            color: var(--light-color);
        }
        
        /* Performance optimized animations */
        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;
            }
        }
        
        /* Typewriter effect */
        .typewriter-container {
            display: inline-block;
            position: relative;
        }
        
        .typed-cursor {
            opacity: 1;
            animation: blink 0.7s infinite;
            color: var(--primary-color);
            font-weight: 700;
        }
        
        @keyframes blink {
            0% { opacity: 1; }
            50% { opacity: 0; }
            100% { opacity: 1; }
        }
        
        /* Hero section with optimized animations */
        .hero-section {
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            isolation: isolate;
        }
        
        .hero-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(78,84,200,0.1) 0%, rgba(143,148,251,0.05) 100%);
            z-index: -2;
        }
        
        .floating-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1;
            will-change: transform;
        }
        
        .shape {
            position: absolute;
            opacity: 0.1;
            border-radius: 50%;
            filter: blur(60px);
            animation: float 15s infinite ease-in-out;
            will-change: transform;
            backface-visibility: hidden;
        }
        
        .shape-1 {
            width: min(30vw, 300px);
            height: min(30vw, 300px);
            background: var(--primary-color);
            top: -10vh;
            right: -10vw;
            animation-delay: 0s;
        }
        
        .shape-2 {
            width: min(20vw, 200px);
            height: min(20vw, 200px);
            background: var(--secondary-color);
            bottom: -5vh;
            left: -5vw;
            animation-delay: 2s;
        }
        
        .shape-3 {
            width: min(15vw, 150px);
            height: min(15vw, 150px);
            background: var(--primary-color);
            top: 50%;
            left: 30%;
            animation-delay: 4s;
        }
        
        @keyframes float {
            0%, 100% {
                transform: translate(0, 0);
            }
            25% {
                transform: translate(1vw, 1.5vh);
            }
            50% {
                transform: translate(2vw, 0.5vh);
            }
            75% {
                transform: translate(1vw, 1.5vh);
            }
        }
        
        .hero-content {
            position: relative;
            z-index: 2;
            max-width: var(--max-content-width);
            margin: 0 auto;
            padding: 0 2rem;
            width: 100%;
        }
        
        .hero-title {
            font-weight: 700;
            font-size: clamp(2.5rem, 5vw, 4.5rem);
            margin-bottom: 1.5rem;
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            line-height: 1.1;
            font-family: 'Montserrat', system-ui, sans-serif;
            text-shadow: 0 2px 10px rgba(78, 84, 200, 0.2);
            letter-spacing: -0.05em;
        }
        
        .hero-subtitle {
            font-size: clamp(1.2rem, 2vw, 1.8rem);
            margin-bottom: 2rem;
            opacity: 0.9;
            font-weight: 500;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }
        
        /* Button Styles with improved accessibility */
        .btn-primary-custom {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
            border: none;
            padding: 0.75rem 1.75rem;
            font-weight: 600;
            border-radius: 50px;
            box-shadow: var(--box-shadow);
            transition: all var(--transition-time) ease;
            color: white !important;
            position: relative;
            overflow: hidden;
            z-index: 1;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            min-width: 180px;
            text-align: center;
        }
        
        .btn-primary-custom:hover,
        .btn-primary-custom:focus {
            transform: translateY(-3px);
            box-shadow: var(--box-shadow-hover);
            color: white !important;
        }
        
        .btn-primary-custom::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--primary-dark), var(--primary-color));
            z-index: -1;
            opacity: 0;
            transition: opacity var(--transition-time) ease;
        }
        
        .btn-primary-custom:hover::after,
        .btn-primary-custom:focus::after {
            opacity: 1;
        }
        
        .btn-outline-custom {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            padding: 0.65rem 1.75rem;
            font-weight: 600;
            border-radius: 50px;
            transition: all var(--transition-time) ease;
            position: relative;
            overflow: hidden;
            z-index: 1;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            min-width: 180px;
            text-align: center;
        }
        
        .btn-outline-custom:hover,
        .btn-outline-custom:focus {
            color: white;
            transform: translateY(-3px);
        }
        
        .btn-outline-custom::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
            z-index: -1;
            opacity: 0;
            transition: opacity var(--transition-time) ease;
        }
        
        .btn-outline-custom:hover::before,
        .btn-outline-custom:focus::before {
            opacity: 1;
        }
        
        /* Social links with progressive enhancement */
        .social-links {
            margin-top: 3rem;
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 3.5rem;
            height: 3.5rem;
            border-radius: 50%;
            background-color: rgba(78, 84, 200, 0.1);
            color: var(--primary-color);
            transition: all var(--transition-time) ease;
            font-size: 1.25rem;
            position: relative;
            overflow: hidden;
        }
        
        .social-links a::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
            z-index: -1;
            transform: scale(0);
            transition: transform var(--transition-time) ease;
            border-radius: 50%;
        }
        
        .social-links a:hover,
        .social-links a:focus {
            color: white;
            transform: translateY(-5px);
            background-color: transparent;
        }
        
        .social-links a:hover::after,
        .social-links a:focus::after {
            transform: scale(1);
        }
        
        /* Scroll down arrow with reduced motion alternative */
        .scroll-down {
            position: absolute;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            color: var(--primary-color);
            animation: bounce 2s infinite;
            cursor: pointer;
            z-index: 10;
            width: 3rem;
            height: 3rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.2);
            transition: all var(--transition-time) ease;
            border: none;
            outline: none;
        }
        
        .scroll-down:hover,
        .scroll-down:focus {
            color: white;
            background-color: var(--primary-color);
            transform: translateX(-50%) scale(1.1);
        }
        
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0) translateX(-50%);
            }
            40% {
                transform: translateY(-20px) translateX(-50%);
            }
            60% {
                transform: translateY(-10px) translateX(-50%);
            }
        }
        
        /* Reduced motion alternative */
        @media (prefers-reduced-motion: reduce) {
            .scroll-down {
                animation: none;
            }
        }
        
        /* Focus styles for accessibility */
        a:focus-visible,
        button:focus-visible,
        input:focus-visible,
        textarea:focus-visible,
        select:focus-visible {
            outline: 2px solid var(--primary-color);
            outline-offset: 2px;
        }
        
        /* Skip to content link for keyboard users */
        .skip-to-content {
            position: absolute;
            top: 0;
            left: 0;
            padding: 0.5rem 1rem;
            background: var(--primary-color);
            color: white;
            z-index: 9999;
            transform: translateY(-100%);
            transition: transform 0.3s ease;
        }
        
        .skip-to-content:focus {
            transform: translateY(0);
        }
        
        /* Responsive adjustments with mobile-first approach */
        @media (max-width: 768px) {
            :root {
                --header-height: 70px;
            }
            
            .hero-title {
                font-size: 2.5rem;
                margin-bottom: 1rem;
            }
            
            .hero-subtitle {
                font-size: 1.2rem;
                margin-bottom: 1.5rem;
            }
            
            .btn-primary-custom,
            .btn-outline-custom {
                min-width: 140px;
                padding: 0.6rem 1.25rem;
                font-size: 0.9rem;
            }
            
            .social-links a {
                width: 2.75rem;
                height: 2.75rem;
                font-size: 1rem;
            }
            
            .shape {
                filter: blur(40px);
            }
        }
        
        @media (max-width: 480px) {
            .hero-title {
                font-size: 2rem;
            }
            
            .hero-subtitle {
                font-size: 1.1rem;
            }
            
            .btn-group {
                flex-direction: column;
                gap: 1rem;
            }
            
            .btn-primary-custom,
            .btn-outline-custom {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Skip to content link for accessibility -->
    <a href="#main-content" class="skip-to-content">Skip to main content</a>
    
    <jsp:include page="navbar.jsp" />
    
    <main id="main-content">
        <section class="hero-section" aria-labelledby="hero-heading">
            <!-- Background animation shapes -->
            <div class="floating-shapes" aria-hidden="true">
                <div class="shape shape-1"></div>
                <div class="shape shape-2"></div>
                <div class="shape shape-3"></div>
            </div>
            
            <div class="container hero-content">
                <div class="row justify-content-center">
                    <div class="col-lg-8 text-center">
                        <h6 class="text-uppercase text-primary mb-3 animate__animated animate__fadeIn" data-aos="fade-down">Hello, I'm</h6>
                        <h1 class="hero-title animate__animated animate__fadeInUp" data-aos="fade-up" id="hero-heading">Ritika</h1>
                        <h2 class="hero-subtitle animate__animated animate__fadeInUp animate__delay-1s" data-aos="fade-up" data-aos-delay="200">
                            <span class="typewriter-container">
                                <span class="typewriter" id="typewriter" aria-live="polite"></span>
                                <span class="typed-cursor" aria-hidden="true">|</span>
                            </span>
                        </h2>
                        <br/>
                        <p class="lead mb-4 animate__animated animate__fadeInUp animate__delay-2s" data-aos="fade-up" data-aos-delay="400">
                            I build robust, scalable web applications with modern technologies and clean code practices.
                        </p>
                        
                        <div class="d-flex flex-wrap justify-content-center gap-3 animate__animated animate__fadeInUp animate__delay-3s" data-aos="fade-up" data-aos-delay="600">
                            <a href="/resume.pdf" class="btn btn-primary-custom" download="Ritika-Java-Developer-Resume.pdf">
                                <i class="fas fa-download me-2" aria-hidden="true"></i> Download Resume
                            </a>
                            <a href="#contact" class="btn btn-outline-custom">
                                <i class="fas fa-paper-plane me-2" aria-hidden="true"></i> Contact Me
                            </a>
                        </div>
                        
                        <div class="social-links animate__animated animate__fadeInUp animate__delay-4s" data-aos="fade-up" data-aos-delay="800" aria-label="Social media links">
                            <a href="#" target="_blank" rel="noopener noreferrer" aria-label="GitHub profile">
                                <i class="fab fa-github" aria-hidden="true"></i>
                            </a>
                            <a href="#" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn profile">
                                <i class="fab fa-linkedin-in" aria-hidden="true"></i>
                            </a>
                            <a href="#" target="_blank" rel="noopener noreferrer" aria-label="Twitter profile">
                                <i class="fab fa-twitter" aria-hidden="true"></i>
                            </a>
                            <a href="#" aria-label="Send email">
                                <i class="fas fa-envelope" aria-hidden="true"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            
            <a href="#about" class="scroll-down animate__animated animate__fadeIn animate__delay-5s" data-aos="fade-up" data-aos-delay="1000" aria-label="Scroll down">
                <i class="fas fa-chevron-down" aria-hidden="true"></i>
            </a>
        </section>
        
        <!-- Additional sections would go here -->
    </main>
    
    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    
    <!-- AOS (Animate On Scroll) -->
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    
    <!-- Performance optimized scripts -->
    <script>
        // Initialize AOS after DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            AOS.init({
                duration: 800,
                easing: 'ease-in-out',
                once: false,
                mirror: true,
                disable: window.matchMedia('(prefers-reduced-motion: reduce)').matches
            });
            
            // Typewriter effect
            const typewriterElement = document.getElementById('typewriter');
            const professions = ['Full Stack Developer', 'Spring Boot Expert', 'Web Application Developer', 'Software Engineer'];
            let professionIndex = 0;
            let charIndex = 0;
            let isDeleting = false;
            let typingSpeed = 100;
            
            function typeWriter() {
                const currentProfession = professions[professionIndex];
                
                if (isDeleting) {
                    typewriterElement.textContent = currentProfession.substring(0, charIndex - 1);
                    charIndex--;
                    typingSpeed = 50;
                } else {
                    typewriterElement.textContent = currentProfession.substring(0, charIndex + 1);
                    charIndex++;
                    typingSpeed = 100;
                }
                
                if (!isDeleting && charIndex === currentProfession.length) {
                    isDeleting = true;
                    typingSpeed = 1500; // Pause at end
                } else if (isDeleting && charIndex === 0) {
                    isDeleting = false;
                    professionIndex = (professionIndex + 1) % professions.length;
                    typingSpeed = 500; // Pause before typing next
                }
                
                setTimeout(typeWriter, typingSpeed);
            }
            
            // Start the typewriter effect after a slight delay
            setTimeout(typeWriter, 1000);
            
            // Smooth scrolling for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    
                    const targetId = this.getAttribute('href');
                    const targetElement = document.querySelector(targetId);
                    
                    if (targetElement) {
                        targetElement.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                        
                        // Update URL without page jump
                        if (history.pushState) {
                            history.pushState(null, null, targetId);
                        } else {
                            location.hash = targetId;
                        }
                    }
                });
            });
            
            // Dark mode toggle functionality
            const darkModeToggle = document.getElementById('darkModeToggle');
            const body = document.body;
            
            // Check for saved user preference or system preference
            const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');
            const currentTheme = localStorage.getItem('theme');
            
            if (currentTheme === 'dark' || (!currentTheme && prefersDarkScheme.matches)) {
                body.classList.add('dark-mode');
                if (darkModeToggle) darkModeToggle.checked = true;
            }
            
            // Listen for system theme changes
            prefersDarkScheme.addListener(e => {
                if (!localStorage.getItem('theme')) {
                    if (e.matches) {
                        body.classList.add('dark-mode');
                        if (darkModeToggle) darkModeToggle.checked = true;
                    } else {
                        body.classList.remove('dark-mode');
                        if (darkModeToggle) darkModeToggle.checked = false;
                    }
                }
            });
            
            // Enhanced background animation with performance checks
            let lastAnimationFrameTime = 0;
            const animationInterval = 1000 / 30; // Target 30fps
            
            document.addEventListener('mousemove', (e) => {
                const now = performance.now();
                if (now - lastAnimationFrameTime < animationInterval) return;
                lastAnimationFrameTime = now;
                
                const shapes = document.querySelectorAll('.shape');
                const x = e.clientX / window.innerWidth;
                const y = e.clientY / window.innerHeight;
                
                shapes.forEach((shape, index) => {
                    const speed = index * 0.3 + 1;
                    shape.style.transform = `translate(${x * 30 * speed}px, ${y * 30 * speed}px)`;
                });
            });
            
            // Service worker registration for PWA
            if ('serviceWorker' in navigator) {
                window.addEventListener('load', () => {
                    navigator.serviceWorker.register('/sw.js').then(registration => {
                        console.log('ServiceWorker registration successful');
                    }).catch(err => {
                        console.log('ServiceWorker registration failed: ', err);
                    });
                });
            }
            
            // Preload images that will be needed soon
            function preloadImages() {
                const images = [
                    '/images/profile.jpg',
                    '/images/project1.jpg',
                    '/images/project2.jpg'
                ];
                
                images.forEach(src => {
                    const img = new Image();
                    img.src = src;
                });
            }
            
            // Start preloading after initial page load
            setTimeout(preloadImages, 1000);
        });
        
        // Track page views with minimal performance impact
        window.addEventListener('load', function() {
            if (navigator.sendBeacon && window.performance) {
                const data = new FormData();
                data.append('url', window.location.href);
                data.append('load_time', window.performance.timing.loadEventEnd - window.performance.timing.navigationStart);
                navigator.sendBeacon('/analytics', data);
            }
        });
    </script>
    
    <!-- Lazy load non-critical JS -->
    <script defer src="https://cdn.jsdelivr.net/npm/vanilla-lazyload@17.8.3/dist/lazyload.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const lazyLoadInstance = new LazyLoad({
                elements_selector: ".lazy",
                threshold: 200,
                callback_loaded: function(el) {
                    el.classList.add('lazy-loaded');
                }
            });
        });
    </script>
</body>
</html>
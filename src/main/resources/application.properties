# Server configuration
server.port=8080

# JSP configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=false

# Default email credentials (used with "kuku" username)
app.mail.default.username=driveuploadmicromax281@gmail.com
app.mail.default.password=qntfkrrluktmednb

# WebSocket configuration
spring.mvc.async.request-timeout=60000
spring.websocket.max-text-message-size=8192
spring.websocket.max-binary-message-size=8192

# Multipart file upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Disable EL identifier check to allow 'this' keyword
org.apache.el.parser.SKIP_IDENTIFIER_CHECK=true

# Jakarta Mail provider configuration
mail.util.StreamProvider.class=org.eclipse.angus.mail.util.DefaultStreamProvider
mail.mime.StreamProvider.class=org.eclipse.angus.mail.util.DefaultStreamProvider